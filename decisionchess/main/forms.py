from django import forms

# TODO consider whether forms are needed
# class SimpleForm(forms.Form):
    #name = forms.CharField(label="Name", max_length=200)
    #check = forms.BooleanField(required=False)